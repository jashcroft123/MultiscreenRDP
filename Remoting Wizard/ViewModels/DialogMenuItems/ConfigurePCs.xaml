<UserControl x:Class="Remoting_Wizard.ViewModels.DialogMenuItems.ConfigurePCs"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Remoting_Wizard.ViewModels.DialogMenuItems"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation">

    <!--<prism:Dialog.WindowStyle>
        <Style BasedOn="{StaticResource JAWindowStyle}" TargetType="Window" />
    </prism:Dialog.WindowStyle>-->

    <UserControl.Resources>
        <ResourceDictionary />
    </UserControl.Resources>

    <Grid x:Name="LayoutRoot" Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>


        <DataGrid ItemsSource="{Binding ConfigPCs.PCs}">
            
        </DataGrid>
        
        <Button Command="{Binding CloseDialogCommand}" CommandParameter="true" Content="OK"
                Grid.Row="1" Height="25" HorizontalAlignment="Center"
                IsDefault="True" Margin="0,10,0,0" Width="75" />
    </Grid>
</UserControl>
